[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "openradix_graph"
version = "5.0.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
